[package]
name = "crdt_utils"
version = "0.1.0"
edition = "2021"
description = "Conflict-free Replicated Data Types for distributed systems"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
