[package]
name = "language_rotator"
version = "0.1.0"
edition = "2021"
authors = ["AllToolkit"]
description = "Language rotation scheduler with weighted preferences and round-robin selection"
license = "MIT"
keywords = ["language", "rotation", "scheduler", "round-robin"]
categories = ["utility", "algorithms"]
repository = "https://github.com/AllToolkit/AllToolkit"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]