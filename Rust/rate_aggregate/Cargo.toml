[package]
name = "rate_aggregate"
version = "1.0.0"
edition = "2021"
authors = ["AllToolkit"]
description = "Sliding window rate aggregator with percentile calculations"
license = "MIT"
keywords = ["rate", "aggregate", "sliding-window", "percentile", "metrics"]
categories = ["utility", "algorithms"]
repository = "https://github.com/AllToolkit/AllToolkit"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
criterion = "0.5"