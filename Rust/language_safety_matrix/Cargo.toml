[package]
name = "language_safety_matrix"
version = "0.1.0"
edition = "2021"
authors = ["AllToolkit"]
description = "Memory & Type Safety Analysis Tool — comparative safety matrix across programming languages"
license = "MIT"
keywords = ["safety", "memory", "type-system", "analysis", "matrix"]
categories = ["utility", "algorithms", "development-tools"]
repository = "https://github.com/AllToolkit/AllToolkit"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"