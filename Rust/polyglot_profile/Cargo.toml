[package]
name = "polyglot_profile"
version = "0.1.0"
edition = "2021"
authors = ["AllToolkit"]
description = "Cross-language idiomatic code profiler — runs benchmarks in 8 languages and renders a comparative report"
license = "MIT"
keywords = ["polyglot", "benchmark", "profile", "cross-language", "performance"]
categories = ["utility", "algorithms"]
repository = "https://github.com/AllToolkit/AllToolkit"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]