[package]
name = "syntax_translator"
version = "0.1.0"
edition = "2021"
authors = ["AllToolkit"]
description = "Cross-language syntax translator — converts code snippets between any two languages in the rotation"
license = "MIT"
keywords = ["syntax", "translator", "cross-language", "code-conversion"]
categories = ["utility", "algorithms"]
repository = "https://github.com/AllToolkit/AllToolkit"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
regex = "1.0"

[dev-dependencies]