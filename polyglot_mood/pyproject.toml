[project]
name = "polyglot-mood"
version = "0.1.0"
description = "Cross-language personality & emotional profiling for rotation languages"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
polyglot-mood = "polyglot_mood.__main__:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"